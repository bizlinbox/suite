# Placeholder for Traefik dashboard basic auth
# Generate your own .htpasswd file with:
#   htpasswd -nbB admin "your-strong-password" > traefik/.htpasswd
# Then uncomment the dashboard-auth middleware in docker-compose.yml
# Do not commit real credentials to version control.
